{
  "name": "notion-to-ppt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "convert": "bun run src/index.ts",
    "convert-docx": "bun run src/docx.ts",
    "check": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@marp-team/marp-cli": "^4.2.3",
    "@notionhq/client": "^2.2.15",
    "commander": "^14.0.0",
    "docx": "^9.6.1",
    "image-size": "^2.0.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.3"
  }
}